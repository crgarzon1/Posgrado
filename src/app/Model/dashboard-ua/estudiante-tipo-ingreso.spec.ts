import { EstudianteTipoIngreso } from './estudiante-tipo-ingreso';

describe('EstudianteTipoIngreso', () => {
  it('should create an instance', () => {
    expect(new EstudianteTipoIngreso()).toBeTruthy();
  });
});
