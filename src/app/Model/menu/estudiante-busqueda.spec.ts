import { EstudianteBusqueda } from './estudiante-busqueda';

describe('EstudianteBusqueda', () => {
  it('should create an instance', () => {
    expect(new EstudianteBusqueda()).toBeTruthy();
  });
});
