import { MatriculaEstudiante } from './matricula-estudiante';

describe('MatriculaEstudiante', () => {
  it('should create an instance', () => {
    expect(new MatriculaEstudiante()).toBeTruthy();
  });
});
