import { WebServiceResponse } from './web-service-response';

describe('WebServiceResponse', () => {
  it('should create an instance', () => {
    expect(new WebServiceResponse()).toBeTruthy();
  });
});
