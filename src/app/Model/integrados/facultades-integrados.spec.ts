import { FacultadesIntegrados } from './facultades-integrados';

describe('FacultadesIntegrados', () => {
  it('should create an instance', () => {
    expect(new FacultadesIntegrados()).toBeTruthy();
  });
});
