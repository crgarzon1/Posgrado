import { MateriasIntegrados } from './materias-integrados';

describe('MateriasIntegrados', () => {
  it('should create an instance', () => {
    expect(new MateriasIntegrados()).toBeTruthy();
  });
});
